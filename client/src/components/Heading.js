import React from 'react';
import Button from './Button';

const Heading = () => {
  return (
    <div className="heading">
      <div>Bookings</div>
      <Button />
    </div>
  );
};

export default Heading;
